<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/listFragment">

    <fragment
        android:id="@+id/listFragment"
        android:name="com.example.weatherapp.presentation.list.ListFragment"
        android:label="City list" >
        <action
            android:id="@+id/action_listFragment_to_detailFragment"
            app:destination="@id/detailFragment"
            app:enterAnim="@anim/slide_top_for_detali"
            app:popEnterAnim="@anim/slide_top_for_list" />
    </fragment>
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.weatherapp.presentation.detail.DetailFragment"
        android:label="Weather in city"
        tools:layout="@layout/fragment_detail" >
        <argument
            android:name="cityName"
            app:argType="string" />
    </fragment>
</navigation>